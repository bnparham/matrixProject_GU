        # deet = Determinant(self.matrix)
        # deet = deet.determinantOfMatrix()
        # print(f"deet {deet}")
        # if(deet < 0):
        #     answ = abs(answ) * -1
        # else:
        #     answ = abs(answ)